# Builds and publishes the APKs and the app bundle for a v* tag. Every step is written to survive a
# re-run of the same tag, because a release that fails halfway must be fixable
# by pressing the button again rather than by deleting and re-cutting the tag.
name: Release

on:
  push:
    tags: ["v*"]
  # For a tag whose push built nothing - GitHub skips a push whose head
  # commit message carries a CI-skip marker - or whose run was deleted:
  # `gh workflow run release.yml -f tag=v1.2.3`. The tag goes in as an input
  # rather than as the ref, because a dispatch runs the workflow file of the
  # ref it is started on and an older tag does not have this trigger yet.
  workflow_dispatch:
    inputs:
      tag:
        description: The tag to build, for example v1.2.3
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ inputs.tag || github.ref_name }}

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - run: flutter pub get

      - name: Check the tag against pubspec.yaml
        run: |
          tag="${TAG#v}"
          pub="$(sed -n 's/^version: *\([0-9.]*\).*/\1/p' pubspec.yaml)"
          [[ "$tag" == "$pub" ]] || echo "::warning::tag $tag != pubspec $pub"

      # Without a keystore the build is debug-signed, which is fine for testing
      # but must never look like a finished release, hence the prerelease flag
      # further down.
      - name: Set up signing
        id: signing
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          if [[ -z "${KEYSTORE_BASE64:-}" ]]; then
            echo "::warning::no KEYSTORE_BASE64 secret - building a debug-signed APK"
            echo "signed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          base64 -d <<< "$KEYSTORE_BASE64" > android/app/upload-keystore.jks
          cat > android/key.properties <<PROPS
          storeFile=upload-keystore.jks
          storePassword=$KEYSTORE_PASSWORD
          keyAlias=$KEY_ALIAS
          keyPassword=$KEYSTORE_PASSWORD
          PROPS
          echo "signed=true" >> "$GITHUB_OUTPUT"

      # The drift tables and the localisations are generated, and generated
      # code is not in the repo. Without this the build fails on a fresh
      # checkout with a wall of missing types.
      - run: dart run build_runner build --delete-conflicting-outputs

      - run: flutter build apk --release --split-per-abi

      # The Play Store takes only an app bundle; it is signed with the same
      # upload key, Play App Signing re-signs it for distribution.
      - run: flutter build appbundle --release

      - name: Collect artifacts and checksums
        id: collect
        run: |
          mkdir -p artifacts
          for f in build/app/outputs/flutter-apk/*-release.apk; do
            cp "$f" "artifacts/vorsorgeheft-${TAG}-$(basename "$f" | sed 's/app-//;s/-release\.apk//').apk"
          done
          cp build/app/outputs/bundle/release/app-release.aab \
            "artifacts/vorsorgeheft-${TAG}.aab"
          {
            echo "| Datei | SHA256 |"
            echo "|---|---|"
            for f in artifacts/*.apk artifacts/*.aab; do
              echo "| $(basename "$f") | \`$(sha256sum "$f" | cut -d' ' -f1)\` |"
            done
          } | tee -a "$GITHUB_STEP_SUMMARY" > artifacts/SHA256SUMS.md

      - name: Resolve release notes
        run: |
          if [[ -f "release-notes-${TAG}.md" ]]; then
            cp "release-notes-${TAG}.md" notes.md
          else
            git tag -l --format='%(contents)' "$TAG" > notes.md
          fi
          cat artifacts/SHA256SUMS.md >> notes.md

      - name: Publish
        env:
          GH_TOKEN: ${{ github.token }}
          PRERELEASE: ${{ steps.signing.outputs.signed == 'true' && ' ' || '--prerelease' }}
        run: |
          gh release view "$TAG" >/dev/null 2>&1 \
            || gh release create "$TAG" --title "$TAG" --notes-file notes.md $PRERELEASE
          gh release upload "$TAG" artifacts/*.apk artifacts/*.aab --clobber
